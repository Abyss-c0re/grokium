# CubalC host ops (used by Grokium)

Runtime lives in **CubalC**: https://github.com/Abyss-c0re/cubalc  

Prove:

```bash
make -C deps/cubalc test
```

SYS surface: READ WRITE ENV EXIST WHICH HTTP SPAWN JOIN JSON CHAT ARG  
See upstream `AGENTS.md` / `docs/FOR_AGENTS.md`.
